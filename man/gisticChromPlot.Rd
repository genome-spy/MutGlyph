% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gistic.R
\name{gisticChromPlot}
\alias{gisticChromPlot}
\title{Plot GISTIC scores across the genome}
\usage{
gisticChromPlot(
  gistic = NULL,
  fdrCutOff = 0.1,
  markBands = NULL,
  color = NULL,
  ref.build = "hg19",
  txtSize = 0.8,
  cytobandTxtSize = 0.6,
  y_lims = NULL,
  chromosomeTrack = c("strip", "axis"),
  region = NULL,
  nonSignificantColor = NULL,
  annotations = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL,
  annotationTracks = NULL
)
}
\arguments{
\item{gistic}{A maftools \code{GISTIC} object created by
\code{\link[maftools:readGistic]{maftools::readGistic()}}.}

\item{fdrCutOff}{GISTIC q-value cutoff in \verb{(0, 1]} used to assign the two
opaque score colors.}

\item{markBands}{Cytobands to label, \code{"all"} for every significant band, or
\code{NULL} for the five bands with the lowest q-values.}

\item{color}{Two colors for amplification and deletion. An unnamed vector is
interpreted in that order; a named vector must contain \code{Amp} and \code{Del}.}

\item{ref.build}{Reference assembly: \code{"hg18"}, \code{"hg19"}, or \code{"hg38"}.}

\item{txtSize}{Relative cytoband-label size.}

\item{cytobandTxtSize}{Relative chromosome-label size.}

\item{y_lims}{Optional two-element increasing range. Its largest absolute
value sets the common amplification/deletion G-score limit.}

\item{chromosomeTrack}{Chromosome context: \code{"strip"} draws the compact
chromosome bar between the profiles; \code{"axis"} omits the bar and draws a
regular genomic axis below the deletion profile. The latter is useful for
inspecting a closely zoomed region.}

\item{region}{Optional initial genomic region such as
\code{"chr21:39000000-41000000"}, or a chromosome such as \code{"chr21"}. Commas
and whitespace in coordinates are accepted. The endpoints may span
chromosomes, for example \code{"chr3:43393228-chr4:8534670"}. The plot remains
zoomable.}

\item{nonSignificantColor}{One color applied to both event types, or two
colors for amplification and deletion intervals that do not pass
\code{fdrCutOff}. An unnamed vector is interpreted in that order; a named vector
must contain \code{Amp} and \code{Del}. With the default \code{color}, the defaults are
opaque pale red and blue resembling the former translucent colors. If
\code{color} is customized, the fallback is \code{"lightgray"} for both.}

\item{annotations}{Optional data frame of custom genomic labels with columns
\code{chromosome}, \code{start}, \code{end}, \code{label}, and \code{event_type}. Event type must be
\code{"Amp"} or \code{"Del"}. Labels are anchored to the highest overlapping
G-score interval.}

\item{width, height}{Widget dimensions.}

\item{elementId}{Optional element ID.}

\item{annotationTracks}{Optional named list of \code{GRanges} or data-frame
interval tracks to show below the GISTIC profiles. Scores prioritize
labels; the built-in \code{\link[=mutglyph_gene_annotations]{mutglyph_gene_annotations()}} resource is one option.}
}
\value{
A MutGlyph htmlwidget.
}
\description{
Creates an interactive GenomeSpy counterpart to
\code{\link[maftools:gisticChromPlot]{maftools::gisticChromPlot()}}. Amplification scores grow upward, deletion
scores grow downward, and a zoom-aware chromosome strip separates them.
}
\details{
The common G-score domain makes amplification and deletion magnitudes
directly comparable. Every score interval is drawn once with an opaque
color: \code{color} for intervals passing \code{fdrCutOff} and
\code{nonSignificantColor} for the remaining context. This avoids alpha blending
where narrow rectangles overlap. Tooltips include score, q-value, frequency,
amplitude, and genomic coordinates.

Only compact score and cytoband-summary fields are embedded in the widget;
the large per-sample lesion matrix in the GISTIC object is not serialized.
}
\examples{
if (interactive()) {
  gistic <- maftools::readGistic(
    gisticDir = system.file("extdata", package = "maftools"),
    isTCGA = TRUE,
    verbose = FALSE
  )
  gisticChromPlot(gistic, markBands = "all")
}
}
\seealso{
\code{\link[=oncoplot]{oncoplot()}}, \code{\link[maftools:readGistic]{maftools::readGistic()}}, and \code{\link[=as_json]{as_json()}}.
}
