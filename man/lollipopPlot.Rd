% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lollipop.R
\name{lollipopPlot}
\alias{lollipopPlot}
\title{Draw an interactive protein lollipop plot}
\usage{
lollipopPlot(
  maf,
  data = NULL,
  gene = NULL,
  AACol = NULL,
  labelPos = NULL,
  labPosSize = 0.9,
  showMutationRate = TRUE,
  showDomainLabel = TRUE,
  refSeqID = NULL,
  proteinID = NULL,
  colors = NULL,
  domains = NULL,
  proteinLength = NULL,
  layout = c("basic", "displaced"),
  count = c("events", "samples"),
  minCount = NULL,
  yScale = NULL,
  showLegend = TRUE,
  collapsePosLabel = TRUE,
  labPosAngle = 0,
  pointSize = 1.5,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{maf}{A maftools \code{MAF} object.}

\item{data}{Optional mutation data frame, following \code{maftools::lollipopPlot()}.
MutGlyph accepts its two-column position/count convention as well as named
\code{position}, \code{mutation}, \code{variant_class} (or \code{classification}), \code{sample},
\code{gene}, and \code{count} columns.}

\item{gene}{One gene symbol. Required for MAF input; optional when a custom
data frame contains exactly one value in its \code{gene} column.}

\item{AACol}{Optional column containing protein changes. With MAF input,
\code{HGVSp_Short}, \code{Protein_Change}, and \code{AAChange} are tried in that order.}

\item{labelPos}{Amino-acid positions to label, or \code{"all"}. The displaced
layout labels all mutations by default; the basic layout labels none.}

\item{labPosSize}{Relative mutation-label size, matching maftools' \code{cex}
semantics.}

\item{showMutationRate}{Include the mutated-sample fraction in the title.}

\item{showDomainLabel}{Draw ranged labels inside protein domains.}

\item{refSeqID, proteinID}{Optional RefSeq transcript or protein identifier.
These retain maftools' domain-selection semantics. If compatible RefSeq
metadata is present in the mutation input, MutGlyph warns about mixed or
mismatching isoforms without silently filtering mutations.}

\item{colors}{Optional named character vector overriding mutation-class
colors.}

\item{domains}{Optional custom domain data frame with \code{start}, \code{end}, and
\code{label} columns. \code{description} and \code{protein_length} are optional.}

\item{proteinLength}{Optional protein length in amino acids.}

\item{layout}{Either \code{"basic"} for true-position vertical lollipops or
\code{"displaced"} for collision-aware markers, labels, and connectors.}

\item{count}{Use mutation \code{"events"} (the maftools convention) or distinct
tumor \code{"samples"} for recurrence heights. For a pre-aggregated data frame,
this also defines what its \code{count} column represents.}

\item{minCount}{Minimum plotted recurrence under the selected \code{count} mode.
When \code{NULL}, the basic layout uses one and the displaced layout uses two.}

\item{yScale}{\code{"linear"} or \code{"log"}. When \code{NULL}, basic plots use linear
and displaced plots use logarithmic scaling.}

\item{showLegend}{Show the mutation-class legend.}

\item{collapsePosLabel}{Combine mutation labels at the same amino-acid
position, for example \code{D835Y/H}. Used by the basic layout.}

\item{labPosAngle}{Mutation-label angle in degrees. Positive values rotate
counterclockwise, as in maftools. Used by the basic layout.}

\item{pointSize}{Relative marker-size multiplier, matching maftools' linear
\code{cex} semantics.}

\item{width, height}{Widget dimensions.}

\item{elementId}{Optional element ID.}
}
\value{
A MutGlyph htmlwidget.
}
\description{
Creates a GenomeSpy protein-mutation lollipop plot from a \code{maftools} MAF
object or an ordinary data frame. The basic layout follows
\code{\link[maftools:lollipopPlot]{maftools::lollipopPlot()}}, while the displaced layout separates dense
hotspots and connects every marker back to its true protein position.
}
\details{
With MAF input and no \code{domains}, protein domains are read from maftools'
bundled \code{protein_domains.RDs} snapshot. Custom data without domains is drawn
against a plain protein backbone. \code{\link[=mutglyph_interpro_domains]{mutglyph_interpro_domains()}} returns a
compatible domain table when online InterPro annotations are desired.
When no identifier is supplied, MutGlyph follows maftools by selecting the
longest bundled protein model (and the first transcript when lengths tie).
Selected transcript and protein identifiers are shown below the plot title.

Mutation event and distinct-sample counts are both retained in
\code{plot$x$spec$datasets$mutations}, irrespective of which one is plotted.
}
\examples{
if (interactive()) {
  laml <- maftools::read.maf(
    maf = system.file("extdata", "tcga_laml.maf.gz", package = "maftools"),
    verbose = FALSE
  )
  # Frozen InterPro representative-domain matches for UniProt P36888.
  # Fetch current matches with mutglyph_interpro_domains("P36888").
  flt3_domains <- data.frame(
    start = c(246, 438, 564, 756),
    end = c(357, 531, 695, 958),
    label = c("Ig-like", "Ig-like", "Kinase N", "Kinase C"),
    protein_length = 993
  )
  lollipopPlot(
    laml,
    gene = "FLT3",
    AACol = "Protein_Change",
    domains = flt3_domains,
    layout = "displaced"
  )
}
}
\seealso{
\code{\link[=oncoplot]{oncoplot()}}, \code{\link[=rainfallPlot]{rainfallPlot()}}, and \code{\link[=as_json]{as_json()}}.
}
