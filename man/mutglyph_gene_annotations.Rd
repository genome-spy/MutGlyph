% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene-annotations.R
\name{mutglyph_gene_annotations}
\alias{mutglyph_gene_annotations}
\title{Load MutGlyph's scored gene-body annotation track}
\usage{
mutglyph_gene_annotations(ref.build = "hg19")
}
\arguments{
\item{ref.build}{Reference assembly: \code{"hg18"}, \code{"hg19"}, or \code{"hg38"}.}
}
\value{
A \code{GenomicRanges::GRanges} object with \code{symbol}, \code{gene_id}, and
\code{score} metadata columns.
}
\description{
Loads the offline, assembly-specific gene-body resource prepared from UCSC
RefSeq and NCBI annotations. Coordinates come from UCSC RefSeq \code{refGene}
records mapped through \code{ncbiRefSeqLink}; symbols come from NCBI \code{gene_info}.
The score is a popularity score counting unique GeneID--PubMed pairs in
NCBI GeneRIF \code{generifs_basic}. It is used only to prioritize labels, not as
a measure of biological importance.
}
\examples{
if (requireNamespace("GenomicRanges", quietly = TRUE) &&
    requireNamespace("GenomeInfoDb", quietly = TRUE)) {
  genes <- mutglyph_gene_annotations("hg19")
  genes[genes$symbol \%in\% c("TP53", "PIK3CA")]
}
}
