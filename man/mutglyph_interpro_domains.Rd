% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpro-domains.R
\name{mutglyph_interpro_domains}
\alias{mutglyph_interpro_domains}
\title{Retrieve representative protein domains from InterPro}
\usage{
mutglyph_interpro_domains(
  proteinID,
  representative = TRUE,
  cache = TRUE,
  refresh = FALSE,
  cacheDir = tools::R_user_dir("MutGlyph", "cache")
)
}
\arguments{
\item{proteinID}{One UniProt accession, optionally including an isoform
suffix such as \code{"P36888-1"}.}

\item{representative}{Keep only InterPro's representative domain matches.}

\item{cache}{Read and write a user-level cache.}

\item{refresh}{Ignore an existing cached result and request current data.}

\item{cacheDir}{Cache directory. The default is MutGlyph's platform-specific
user cache directory.}
}
\value{
A data frame with \code{start}, \code{end}, \code{label}, \code{description},
\code{accession}, \code{interpro_accession}, \code{source_database}, \code{type},
\code{representative}, \code{protein_id}, and \code{protein_length} columns.
}
\description{
Queries the InterPro API for a UniProt protein and returns a plain domain
table accepted by \code{\link[=lollipopPlot]{lollipopPlot()}}. Results are cached outside the
installed package so subsequent calls can work offline.
}
\details{
Network access is explicit: plotting a returned or custom table never calls
InterPro. For reproducible analyses, save the returned table with
\code{\link[=saveRDS]{saveRDS()}} and reuse that snapshot.

Only entries classified as domains, repeats, or homologous superfamilies are
returned. A discontinuous match produces one row for each fragment.
}
\examples{
\dontrun{
flt3_domains <- mutglyph_interpro_domains("P36888")
lollipopPlot(
  laml,
  gene = "FLT3",
  domains = flt3_domains
)
}
}
