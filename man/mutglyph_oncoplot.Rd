% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oncoplot.R
\name{mutglyph_oncoplot}
\alias{mutglyph_oncoplot}
\title{Draw an interactive oncoplot}
\usage{
mutglyph_oncoplot(
  maf,
  top = 20,
  minMut = NULL,
  altered = FALSE,
  genes = NULL,
  genesToIgnore = NULL,
  colors = NULL,
  keepGeneOrder = FALSE,
  sampleOrder = NULL,
  removeNonMutated = FALSE,
  clinicalFeatures = NULL,
  annotationColor = NULL,
  sortByAnnotation = FALSE,
  annotationOrder = NULL,
  topBarData = NULL,
  topBarLims = NULL,
  leftBarData = NULL,
  leftBarLims = NULL,
  rightBarData = NULL,
  rightBarLims = NULL,
  draw_titv = FALSE,
  titv_col = NULL,
  includeColBarCN = TRUE,
  showTumorSampleBarcodes = FALSE,
  rowHeight = 24,
  drawRowBar = TRUE,
  drawColBar = TRUE,
  showPct = TRUE,
  showTitle = TRUE,
  titleText = NULL,
  width = NULL,
  height = NULL,
  elementId = NULL
)
}
\arguments{
\item{maf}{A maftools \code{MAF} object.}

\item{top}{Number of genes to display when \code{genes} is \code{NULL}.}

\item{minMut}{Optional minimum mutated/altered sample count or cohort fraction.}

\item{altered}{Use altered rather than mutated sample counts for \code{minMut}.}

\item{genes}{Optional gene symbols to display instead of selecting top genes.}

\item{genesToIgnore}{Optional gene symbols removed after selection.}

\item{colors}{Optional named character vector of mutation-class colors.}

\item{keepGeneOrder}{Preserve the supplied/selected gene order.}

\item{sampleOrder}{Optional sample barcodes to select and order.}

\item{removeNonMutated}{Remove samples without events in the displayed genes.}

\item{clinicalFeatures}{Optional categorical clinical fields to display.}

\item{annotationColor}{Optional named list of per-feature categorical color
mappings or GenomeSpy color-scheme names.}

\item{sortByAnnotation}{Sort samples by the selected clinical features.}

\item{annotationOrder}{Optional named list of partial categorical level orders.}

\item{topBarData}{Optional two-column sample metric data or one clinical field.}

\item{topBarLims}{Optional numeric limits for the custom top bar.}

\item{leftBarData, rightBarData}{Optional two-column gene metric data.}

\item{leftBarLims, rightBarLims}{Optional numeric limits for custom side bars.}

\item{draw_titv}{Show a transition/transversion contribution track.}

\item{titv_col}{Optional named character vector of Ti/Tv class colors.}

\item{includeColBarCN}{Include \code{Amp} and \code{Del} gene-level copy-number calls
in the top sample summary bars, matching \code{maftools::oncoplot()}.}

\item{showTumorSampleBarcodes}{Show rotated sample names below the matrix.}

\item{rowHeight}{Height in pixels of each gene row.}

\item{drawRowBar}{Show the stacked mutation-count bars to the right.}

\item{drawColBar}{Show the stacked sample mutation-burden bars above.}

\item{showPct}{Show altered-sample percentages beside the gene rows.}

\item{showTitle}{Show the cohort summary above the plot.}

\item{titleText}{Optional text that replaces the generated cohort summary.}

\item{width, height}{Widget dimensions.}

\item{elementId}{Optional element ID.}
}
\value{
A MutGlyph htmlwidget.
}
\description{
Creates a GenomeSpy oncoplot from a \code{maftools} MAF object. The initial
implementation follows the default layout and ordering of
\code{maftools::oncoplot()} while retaining the complete GenomeSpy specification
in the returned widget.
}
\details{
The sample axis supports wheel or trackpad zooming and panning. Hover over
mutation cells and summary bars for details, or use the widget's \strong{Save as
SVG} button to export the currently visible composition.

Categorical and numeric clinical annotations use independent color scales.
Sample labels use ranged text and appear only when their bands are wide
enough; zooming in reveals labels hidden in a dense overview. Custom summary
bars accept one numeric metric keyed by sample or gene, and use the metric's
column name as the axis title.
}
\examples{
if (interactive()) {
  laml <- maftools::read.maf(
    maf = system.file("extdata", "tcga_laml.maf.gz", package = "maftools"),
    clinicalData = system.file(
      "extdata",
      "tcga_laml_annot.tsv",
      package = "maftools"
    )
  )
  mutglyph_oncoplot(laml, top = 10)
}
}
\seealso{
\code{\link[=as_json]{as_json()}} to retrieve the rendered GenomeSpy specification.
}
