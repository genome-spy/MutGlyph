% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rainfall.R
\name{rainfallPlot}
\alias{rainfallPlot}
\title{Draw an interactive rainfall plot}
\usage{
rainfallPlot(
  maf,
  tsb = NULL,
  detectChangePoints = FALSE,
  ref.build = "hg19",
  color = NULL,
  savePlot = FALSE,
  width = NULL,
  height = NULL,
  fontSize = 1.2,
  pointSize = 0.4,
  region = NULL,
  elementId = NULL
)
}
\arguments{
\item{maf}{A maftools \code{MAF} object.}

\item{tsb}{One tumor sample barcode. When \code{NULL}, the most mutated sample is
selected.}

\item{detectChangePoints}{Detect and annotate potential kataegis loci using
the maftools criterion: at least six consecutive mutations with an average
intermutation distance no greater than 1,000 bp.}

\item{ref.build}{Reference assembly: \code{"hg18"}, \code{"hg19"}, or \code{"hg38"}.}

\item{color}{Optional named character vector overriding colors for the six
pyrimidine substitution classes.}

\item{savePlot}{Retained for maftools API familiarity. \code{TRUE} is unsupported;
use the widget's \strong{Save as SVG} button instead.}

\item{width, height}{Widget dimensions. Unlike maftools, these do not describe
a PDF device.}

\item{fontSize}{Relative font-size multiplier.}

\item{pointSize}{Relative mutation-point size.}

\item{region}{Optional initial genomic region such as
\code{"chr8:98000000-98500000"}, or a chromosome such as \code{"chr8"}. Commas and
whitespace in coordinates are accepted. The endpoints may span
chromosomes, for example \code{"chr3:43393228-chr4:8534670"}. The plot remains
zoomable.}

\item{elementId}{Optional element ID.}
}
\value{
A MutGlyph htmlwidget.
}
\description{
Creates a GenomeSpy rainfall plot from a \code{maftools} MAF object. The API
deliberately follows \code{\link[maftools:rainfallPlot]{maftools::rainfallPlot()}} where practical, making the
function an almost drop-in interactive replacement.
}
\details{
Only SNP records are plotted, including synonymous variants. Substitutions
are normalized to the six pyrimidine classes. Inter-event distances are
calculated independently within each chromosome, so chromosome boundaries
do not create artificial distances.

With \code{detectChangePoints = TRUE}, detected kataegis intervals are shaded and
marked with upward arrows. The complete locus table is retained in
\code{plot$x$spec$datasets$kataegis} and no files are written as a side effect.
}
\examples{
if (interactive()) {
  brca <- maftools::read.maf(
    maf = system.file("extdata", "brca.maf.gz", package = "maftools"),
    verbose = FALSE
  )
  rainfallPlot(brca, detectChangePoints = TRUE)
}
}
\seealso{
\code{\link[=oncoplot]{oncoplot()}} and \code{\link[=as_json]{as_json()}}.
}
